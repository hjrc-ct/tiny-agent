mode: standalone

rootUser: minioadmin
rootPassword: ${MINIOROOTPWD}

replicas: 1

persistence:
  enabled: true
  size: 20Gi

resources:
  requests:
    cpu: 250m
    memory: 512Mi
  limits:
    cpu: "1"
    memory: 1Gi

service:
  type: ClusterIP

consoleService:
  type: ClusterIP